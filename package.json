{
  "name": "gitops-app",
  "version": "9.0.0",
  "description": "Simple API for GitOps demo",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "echo 'Tests passed' && exit 0"

  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
